{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "ignore": [
    "backend",
    "api",
    "SENTIO AI",
    ".venv",
    ".env"
  ]
}
